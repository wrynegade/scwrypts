#####################################################################

DEPENDENCIES+=(vim)

REQUIRED_ENV+=()

#####################################################################

VIM() { vim $@ </dev/tty >/dev/tty; }

VIM_CONFIG_DIR="${XDG_CONFIG_HOME:-${HOME}/.config}/vim"
[ -d "${HOME}/.vim" ] && VIM_CONFIG_DIR="${HOME}/.vim"

#####################################################################
true
